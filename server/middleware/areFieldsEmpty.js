// const {body} = require('express-validator');

// function areFieldsEmpty(next)  {

//   const fields = ['author','content', 'category','title']
//   for(const field in fields){
//       body(field).not().isEmpty().withMessage(`${field} field is Empty`)
//   }
//   next();

// }

// module.exports= areFieldsEmpty